version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm run setup
    # IMPORTANT - Please verify your build commands
    build:
      commands:
        - npm run build-run
        - echo "HELLO WORLD"
  artifacts:
    # IMPORTANT - Please verify your build output directory
    baseDirectory: /
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*